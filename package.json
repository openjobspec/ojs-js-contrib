{
  "name": "@openjobspec/js-contrib",
  "version": "0.4.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "vitest run",
    "lint": "tsc --noEmit --project tsconfig.base.json"
  },
  "devDependencies": {
    "@openjobspec/sdk": "file:vendor/openjobspec-sdk-0.5.0.tgz",
    "typescript": "^5.4.0",
    "vitest": "^3.2.7"
  },
  "engines": {
    "node": ">=18"
  }
}
